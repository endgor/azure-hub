User-agent: *
Allow: /

# Block crawling of API endpoints (they don't provide SEO value)
Disallow: /api/

# Block crawling of data files (they're for the application, not search engines)
Disallow: /data/

# Block crawling of search URLs with query parameters
Disallow: /tools/ip-lookup/?

# Service tag detail pages are intentionally crawlable. Base tags (e.g. /Storage/)
# are indexable reference pages; regional variants (e.g. /Storage.WestEurope) carry a
# noindex meta tag. They must stay crawlable so Google can read that noindex — blocking
# them here would leave them stuck as "crawled, currently not indexed" instead.

# Sitemap
Sitemap: https://azurehub.org/sitemap.xml
